... 
[groups] 
admins = harry,sally  	#定义组，组成员有harry与sally 
[/]                   	#对版本库根路径设置权限，可以设置为需要控制的路径 
@admins = rw          	#admins组中的用户可读、可写权限 
* = r          	#其他所有的人只读，权限可以设置为只读('r')、读写('rw')、无权限('')
